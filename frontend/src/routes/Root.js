import React from 'react';
import Layout from '../components/Layout';

function Root() {
    // What do y'all plan to use Root for? - vinlin
    // Maybe to show if logged in or som idk? - vinlin

    return <Layout />;
}

export default Root;
